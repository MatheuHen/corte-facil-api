# Para usar MongoDB local (requer instalação):
# DB_URI=mongodb://localhost:27017/corte-facil

# Para usar MongoDB Atlas (gratuito, sem instalação necessária):
# Substitua <username>, <password> e <cluster> pelos seus dados do MongoDB Atlas
# DB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/corte-facil?retryWrites=true&w=majority

# Configuração temporária para desenvolvimento (usando banco local)
DB_URI=mongodb://127.0.0.1:27017/corte-facil
PORT= 3000